# Clipo 实施计划

预估总工期 10 周（单人，全职折算）。每个阶段都以"可运行、可验收"为边界，阶段末产出可演示的成果。

## 优先级原则

按你确认的顺序：核心保存与查看 → 小红书/小黑盒 → 搜索与标签 → iOS Shortcut → 浏览器扩展 → 备份导出。

一处需要说明的排序张力：小红书/小黑盒最可靠的登录态来源是浏览器扩展，但扩展排在更后。因此 Phase 3 先用「用户填写 Cookie + 服务端抓取」打通平台支持，Phase 5 的扩展再补上更稳的 DOM 直取路径。两条路径共用同一套适配器契约，后补不返工。

---

## Phase 1 · 基础设施（2 周）

**目标**：空仓库到可登录、可部署的骨架。

**当前状态（2026-09-20）**：Phase 1 代码已实现，本地 SQLite 的认证、设置、迁移和前端静态构建已验证。Docker Compose 配置已提供；当前 WSL 未启用 Docker 集成，容器与 PostgreSQL 实机验收待完成。详见 [构建进度](docs/progress.md)。

任务：
1. 仓库结构与工程化：backend / frontend / extension / docs 四目录，Makefile，pre-commit（ruff + black + eslint）。
2. FastAPI 应用骨架：配置加载（环境变量 > 数据库 > 默认值）、日志、异常处理器、统一错误体。
3. SQLAlchemy 模型与 Alembic 迁移，覆盖 `users`、`refresh_tokens`、`api_tokens`、`user_settings`。
4. 认证：注册、登录、刷新、登出、API Token 的签发与撤销；Argon2id 哈希。
5. Docker Compose：app + db，健康检查，数据卷。
6. Web 设置向导：首次启动检测无管理员时引导创建账号并填写基础配置。

验收：
- `docker-compose up` 后可访问设置向导，创建管理员，登录成功并拿到 JWT。
- Access Token 过期后前端能静默续期。
- `alembic upgrade head` 在空库与已有库上都可重复执行。

风险：无。此阶段是纯确定性工作。

---

## Phase 2 · 核心链路（3 周）

**目标**：通用网页从分享到查看的端到端闭环，验证流水线设计。

**当前状态（2026-09-20）**：Phase 2 代码已实现，离线夹具覆盖通用网页提取、持久化队列、摘要与降级、笔记 API；Web 已提供笔记、队列、详情及分享入口。实际验证记录与 Android / Docker 验收限制见 [构建进度](docs/progress.md)。

任务：
1. `CapturedContent` 数据结构与 Extractor 注册表；实现通用网页适配器（正文提取用 trafilatura/readability 兜底）。
2. Huey 接入，SQL 存储后端；`capture_jobs` 状态机与指数退避重试。
3. LLM 编排层：OpenAI 兼容客户端、提示词模板、结构化输出解析、token 预算截断、失败降级（LLM 不可用时仅保存原文并标记）。
4. 笔记与评论落库；`extraction_cache` 去重。
5. API：`POST /captures`、`GET /jobs`、`GET /notes`、`GET /notes/{id}`、`DELETE /notes/{id}`。
6. PWA 首版：登录页、笔记列表、笔记详情、保存队列页、设置页（LLM 配置）。
7. Web App Manifest 与 `share_target`，Android 分享即保存。
8. 端到端测试：从提交 URL 到笔记可见的集成测试，用本地固定 HTML 夹具，不依赖外网。

验收：
- 在 Android 上从浏览器分享一个普通网页，队列页可见处理进度，完成后详情页显示摘要与要点。
- LLM Key 未配置时，仍能保存原文，页面明确提示"未生成摘要"。
- 抓取失败的任务可在队列页手动重试。

风险：通用正文提取质量参差。缓解：保留原始 HTML 快照字段，允许重新提取。

---

## Phase 3 · 平台专项（2 周）

**目标**：小红书与小黑盒的内容与评论可用。

**当前状态（2026-09-21）**：已提供小红书、小黑盒 Cookie 的设置接口、按账号加密存储、替换/清除与 Web 获取教程；小红书支持 HTML 帖子、图片、作者、时间、内嵌评论及签名接口顶层评论分页，兼容 `.com` / `.cn` 短链接，安全使用当前账号 Cookie，并区分登录失效/访问限制/频控/结构变化。采集上限独立可配（0–100），评论初筛、LLM 评分、候选上限和高价值阈值已接入；平台请求使用随机选择且单任务内固定的 UA，并控制请求间隔。小黑盒已接入官网/分享链接、正文/图片/作者/时间、签名顶层评论分页和错误诊断；独立 Cookie 有效性探测入口已接入持久化后台任务，并区分登录失效与无法确认；B 站已接入视频元数据、公开可取得的字幕和分页热评，YouTube 仍待完成。离线链路已验证；用户提供的小红书和小黑盒样本已用真实 Cookie/模型完成采集、摘要和评论评分入库，小红书取得 25 条评论及高价值标记，满足 M3 样本验收，但本阶段剩余功能仍待完成，详见 [构建进度](docs/progress.md)。

任务：
1. 设置页新增平台 Cookie 配置，附获取教程；Cookie 加密存储与有效性探测。
2. 小红书适配器：帖子正文、图片、作者、发布时间、评论与点赞数；分页评论抓取上限可配。
3. 小黑盒适配器：同上。
4. 视频类适配器（B 站 / YouTube）：标题、简介、字幕（若可得）、热评。
5. 评论初筛规则：按点赞排序、去重、过滤过短与纯表情、上限 N 条。
6. LLM 评论评分接入与阈值配置；`is_valuable` 落库。
7. 反爬健壮性：随机 UA、请求间隔、失败识别（登录过期 vs 频控 vs 结构变更），错误信息对用户可读。

验收：
- 配置有效 Cookie 后，保存一条小红书帖子，正文、图片与至少 10 条评论入库，其中高价值评论被标记。
- Cookie 失效时，任务失败原因显示为"登录态失效，请更新 Cookie"，而非泛化报错。
- 平台页面结构变更时，只需改动对应适配器文件。

风险：反爬策略随时变化，抓取可能失效。缓解：适配器模块化、契约固定、失败原因可诊断；扩展路径（Phase 5）作为长期更稳方案。

---

## Phase 4 · 检索与移动入口（1.5 周）

**目标**：笔记可被找到，iOS 可录入。

任务：
1. 标签：自动标签（LLM 建议）+ 手动增删，列表页按标签筛选，收藏标记。
2. 全文检索：PostgreSQL `tsvector`（中文分词用 pg_bigm 或 zhparser，缺失时退化为 `LIKE`）；SQLite 走 FTS5。检索层按后端分派。
3. iOS Shortcut：`.shortcut` 文件与配置文档；静默 POST，完成后系统通知；失败通知带原因与打开 App 的入口。
4. PWA 离线：Service Worker 混合策略、最近 50 条预缓存、离线写队列重放、新版本提示条。

验收：
- 中文关键词能命中标题、正文与摘要。
- iOS 分享面板中一次点击完成保存，不打开浏览器。
- 断网后仍可浏览最近笔记；离线期间的删除操作在恢复网络后生效。

风险：中文分词依赖数据库扩展，部分部署环境不具备。缓解：启动时探测能力并降级，文档写明差异。

---

## Phase 5 · 浏览器扩展（1 周）

**目标**：桌面录入，并为受限平台提供可靠抓取路径。

任务：
1. Manifest V3 骨架：background service worker、options 页、popup。
2. 配置项：服务器地址、API Token、连通性测试按钮。
3 右键菜单与 popup 保存：采集 URL、标题、选区文本。
4. 平台内容脚本：小红书/小黑盒 DOM 直取，产出与后端一致的 payload，绕过服务端反爬。
5. 后端 `POST /captures` 支持 `payload` 直传分支，跳过网络抓取。
6. 大内容处理：payload 超阈值时先建 job 再分块上传。

验收：
- 在已登录小红书的浏览器中一键保存，无需任何 Cookie 配置即可拿到完整评论。
- 扩展仅申请最小权限，安装时不出现"读取所有网站数据"提示。

风险：Manifest V3 的 service worker 生命周期短，长任务易被回收。缓解：扩展只负责采集与提交，重活全在后端。

---

## Phase 6 · 备份、文档与发布（0.5 周）

任务：
1. 导出：完整 JSON 与 Markdown 目录；JSON 可回导。
2. 自动备份目标：本地路径、S3 兼容、WebDAV；定时任务与手动触发。
3. 文档定稿：README 演示 GIF、配置参考、部署指南、扩展与 Shortcut 教程。
4. AGPL v3 许可文件与文件头声明、CONTRIBUTING、Issue 模板。
5. CI：lint + 测试 + 镜像构建；发布 v0.1.0 与扩展打包产物。

验收：
- 导出再导入到空实例，笔记、评论、标签、媒体引用完整还原。
- 按 README 在干净机器上 10 分钟内完成部署。

---

## 里程碑一览

| 里程碑 | 阶段 | 标志 |
|--------|------|------|
| M1 可登录可部署 | Phase 1 | 设置向导跑通 |
| M2 端到端闭环 | Phase 2 | 通用网页保存可用 |
| M3 关键平台可用 | Phase 3 | 小红书评论入库 |
| M4 可检索可移动录入 | Phase 4 | 中文搜索 + iOS Shortcut |
| M5 桌面录入 | Phase 5 | 扩展直取 |
| M6 v0.1.0 发布 | Phase 6 | 备份 + 文档 + CI |

## 跨阶段的工程约定

- 每个阶段结束前补齐该阶段的测试，不留到最后。
- 数据库变更一律经 Alembic，禁止手改表结构。
- 适配器新增或修改必须附带离线 HTML 夹具测试，避免依赖线上页面。
- 任何对外错误信息都要能指向用户可执行的下一步动作。

## 主要风险登记

| 风险 | 影响 | 缓解 |
|------|------|------|
| 平台反爬升级导致抓取失效 | 高 | 适配器隔离 + 扩展 DOM 路径 + 可诊断错误 |
| 中文全文检索依赖数据库扩展 | 中 | 能力探测与降级，文档说明 |
| LLM 输出结构不稳定 | 中 | JSON Schema 约束 + 解析失败重试 + 降级只存原文 |
| 媒体存储膨胀 | 中 | 默认只存缩略图，原图可选 |
| 单人维护多端 | 中 | 先 PWA 单一前端，Flutter 端推迟到 MVP 之后 |
